$(function() {
    var codeprint = function() {
        $('code, pre').addClass('prettyprint');
        prettyPrint();
    }();

    var createnav = function() {
        var navString = "";
        $("h1,h2").each(function() {
            if($(this).attr("tagName") == "h1") {

            }
        });
    }();
});
